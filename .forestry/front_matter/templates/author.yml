---
label: Author Post
hide_body: false
fields:
- name: id
  type: text
  config:
    required: true
  label: id
  description: The reference id of the author. Must be the exact same name as the
    title, except all lowercase and replace spaces with a hypen.
- name: title
  type: text
  config:
    required: false
  label: Name
  description: The display name of the author
- name: image
  type: file
  config:
    maxSize: 250
  label: Image
pages:
- authors/mark-jensen.md
- authors/james-tir.md
- authors/john-doe.md
